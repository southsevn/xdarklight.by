module.exports = {
  resolve: {
    // Only for WebStorm @ alias
    alias: {
    // eslint-disable-next-line no-undef
    "@": path.resolve(__dirname),
    // eslint-disable-next-line no-undef
    "~": path.resolve(__dirname)
    }
  }
};
  